linux程序设计 期末考卷回忆版
来源： 蒋苏阳的日志
PS：为了以后的学弟学妹们复习的时候可以稍微轻松一点，有点头绪，特此记录下此试卷的题目。其中，填空记不太清楚，另外三道大题基本记下来了。

 

一：填空（14）

索引结点

几个特殊信号

 

二：简答（38）

1 shell程序设计

查看当前目录下的子目录并只显示子目录；

查找一个错误代码EPERM（宏定义）在Linux系统头文件中的定义并显示。

 

2编写两个简单的程序（fred.c, bill.c），将其编译为目标文件，并分别生成静态库和动态库。再编写程序调用之，说明库的使用。

 

3什么是shell内部命令；试举例说明，并说明该命令为什么是内部命令。

 

4用户没有对/etc/passwd, /etc/shadow的写权限，为什么可以通过passwd命令修改口令。

 

5open系统调用的四种模式分别有什么效果。

 

6以I/O为例，说明系统调用接口与库接口的异同。

 

 

 

三：程序及理解（28）

1创建一个包含两个进程的进程环，其中每一个进程的标准输出是另一个进程的标准输入（画图说明，可以用伪代码，但要写出关键的系统调用函数）。

 

2一段简单的并发网络端程序，填空并说明作用；说出两种实现并发服务器的方式。

 

四：思考与论述（20）

1说明编写一个client_server程序可以使用哪些IPC机制（3种），并对其性能、效率、使用方法、易用性进行述评。

 

2说明shell中管道的实现机制（可以用伪代码，但要写出关键的系统调用函数）